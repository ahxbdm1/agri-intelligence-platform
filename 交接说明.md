# 农智云瞰项目交接说明

更新时间：2026-09-14

本文用于将当前项目交给新的开发者或 AI 助手继续维护。项目根目录为：

`D:\zhhh-sdrjsj-dsj\agri-intelligence-platform`

## 1. 项目定位

项目名称为“农智云瞰：面向县域农业的病虫害识别、产量风险预测与农情决策大数据平台”。目标用户包括县域农业主管部门、农技站、合作社和种植大户，业务主线是：多源农情数据采集，清洗与存储，风险分析，病虫害识别，产量预测，预警，巡检，农资调度，AI 农技问答和日报生成。

当前系统围绕 6 个乡镇、80 个模拟地块构建了完整演示闭环，重点用于“大数据与人工智能行业应用开发”类竞赛展示。所有模拟数据、公开数据集和模型验证边界都已在文档中说明，不能把公开数据指标直接表述为山东本地田间准确率。

## 2. 已完成的系统结构

### 前端

- Next.js 16、React 18、TypeScript、Tailwind CSS。
- Recharts 图表，Leaflet 地图，Lucide 图标。
- GSAP 和 `@gsap/react` 已加入依赖，用于页面微动效。
- 深色农业科技风 App Shell，包含侧边栏、顶部栏、快捷跳转、响应式移动端导航。
- 页面路由：`/login`、`/dashboard`、`/map`、`/disease`、`/predictions`、`/alerts`、`/inspections`、`/assistant`、`/data`、`/reports`。
- 公共组件集中在 `frontend/src/components/`，包括布局、图表、地图、状态、风险标签、统计卡片等。
- 统一样式在 `frontend/src/styles/globals.css`，页面切换时侧栏和整体壳层不应重新闪烁。

### 后端

- FastAPI + SQLAlchemy。
- PostgreSQL 16 为 Docker 默认数据库，SQLite 为本地 fallback。
- JWT 登录认证，演示账号为 `admin / admin123`、`expert / expert123`、`coop / coop123`。
- API 路由文件位于 `backend/app/api/`，覆盖认证、驾驶舱、地块、病虫害、预测、预警、巡检、助手、数据导入导出、报告和调度工作流。
- 自动请求日志、请求 ID、统一 500 错误响应和 FastAPI Swagger 已实现。

### 主要接口

- `POST /api/auth/login`，`GET /api/auth/me`
- `GET /api/dashboard/summary`，`GET /api/dashboard/trends`，`GET /api/dashboard/risk-ranking`
- `GET /api/farms`，`GET /api/farms/{id}`，`GET /api/farms/{id}/risk`
- `POST /api/disease/predict`
- `POST /api/predictions/risk`，`POST /api/predictions/yield`，`GET /api/model/metrics`
- `GET/POST/PATCH /api/alerts...`
- `GET/POST/PATCH /api/inspection-tasks...`
- `GET /api/assistant/status`，`POST /api/assistant/chat`
- `POST /api/data/import`，`GET /api/data/export`
- `POST /api/reports/daily`，`GET /api/reports/{id}`
- `POST /api/workflows/daily-dispatch`

## 3. 数据和数据库状态

演示数据脚本是 `scripts/generate_demo_data.py`，固定随机种子，可重复生成。它会重建本地数据库，因此不要在生产数据库上直接执行，除非明确允许清空数据。

已验证生成规模：6 个乡镇、80 个农田地块、5 种作物、1080 条天气记录、5200 条传感器记录、650 条病虫害上报、400 条产量记录、120 条预警和 206 条巡检任务。

数据实体在 `backend/app/models/entities.py`，包括用户、作物、地块、天气、传感器、病虫害、产量、风险预测、预警、巡检任务、模型指标和知识库文档索引。

## 4. AI 和大数据实现状态

### 病虫害识别

- `backend/ml/disease_classifier.py`：统一识别接口和轻量可训练基线模型。
- `backend/ml/yolo_disease_classifier.py`：YOLO 检测适配器，和基线模型保持相同返回结构。
- `backend/ml/train/`：包含合成数据、IP102 和 PlantDoc 训练入口。
- 当前权重目录：`data/generated/models/`。
- IP102 YOLO11n 权重：`ip102_pest_yolo_best.pt`，102 类，官方测试集 mAP50=0.4133、mAP50-95=0.2597、Precision=0.4761、Recall=0.4572。
- PlantDoc YOLO11n 权重：`plantdoc_yolo_best.pt`，29 类，公开验证集 mAP50=0.6542、mAP50-95=0.5212、Precision=0.6068、Recall=0.6151。
- 轻量基线的合成测试指标为 1.0，只证明工程链路，不代表真实图片泛化能力。
- 前端识别结果包含置信度、严重度、是否人工复核、检测框或疑似区域说明。生产应用仍应增加山东本地标注数据和农技专家复核。

### 风险、产量和异常模型

- `backend/ml/risk_model.py`：RandomForest 风险评分，输出风险分数、等级和主要影响因子。
- `backend/ml/yield_forecast.py`：RandomForest 回归产量预测，输出 MAE、RMSE、R2 和未来曲线数据。
- `backend/ml/anomaly_detector.py`：IsolationForest 传感器异常检测。
- `scripts/train_models.py`：训练、保存模型，并把指标写入数据库。

### RAG 农技助手

- 本地知识库目录：`knowledge_base/`，包含番茄、小麦、水稻、农资防控和天气风险 Markdown。
- 本地检索：`backend/rag/retriever.py`，TF-IDF fallback。
- 远程连接：`backend/rag/ragflow_client.py`，RAGFlow 远程优先，本地知识库降级。
- 当前 `.env` 已配置 RAGFlow 相关变量，但真实密钥只存在本地 `.env`，不要复制到提交包、代码、聊天记录或公开仓库。
- 用 `GET /api/assistant/status` 判断远程是否可用；回答里的 `mode` 为 `ragflow` 或 `local`。服务器实测状态接口可用，但实际问答曾因远端响应超过 35 秒自动降级本地知识库。

### 批处理

- `batch_jobs/pandas_jobs/risk_aggregation_job.py`：可运行的 pandas 批处理，生成乡镇风险排名、作物风险分布、未来风险趋势、产量预测摘要、巡检优先级和农资调度建议。
- `batch_jobs/spark_jobs/risk_aggregation_spark.py`：可选 PySpark 实现，需要 Spark 环境和 Parquet 输入。

## 5. 竞赛材料

Markdown 文档在 `docs/`，已有概要、详细方案、需求分析、数据库设计、概要设计、详细设计、安装部署、使用说明、数据集说明、开源组件说明、测试报告、视频脚本、PPT 大纲、答辩问题、工作计划、会议纪要、工作总结和作品基本信息登记表。

`submission_artifacts/` 中已有可编辑 Word、PDF、PPTX、源代码压缩包和部分页面预览。文件名中的“待填写”“待定团队”需要在正式提交前替换为真实赛题编号、团队编号和团队名称。

仍需人工完成：补录报名信息，替换 PPT 中的现场截图和最终实测指标，录制不超过 8 分钟的 MP4，确认当年官网的提交命名规则和材料格式。

## 6. 本地启动

推荐使用独立 Python 3.11 环境，避免全局 NumPy、SciPy、scikit-learn 版本冲突：

```powershell
cd D:\zhhh-sdrjsj-dsj\agri-intelligence-platform
py -3.11 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r backend\requirements.txt
cd frontend
npm install
cd ..
.\.venv\Scripts\python.exe scripts\generate_demo_data.py
.\.venv\Scripts\python.exe scripts\train_models.py
```

启动后端：

```powershell
$env:PYTHONPATH="backend"
.\.venv\Scripts\python.exe -m uvicorn main:app --app-dir backend --host 0.0.0.0 --port 8000 --reload
```

启动前端：

```powershell
cd frontend
npm run dev
```

本地访问：前端 `http://localhost:3000`，Swagger `http://localhost:8000/docs`。

## 7. Docker 和服务器部署

普通 Docker：

```bash
cp .env.example .env
docker compose up --build
```

服务器部署目录为 `/opt/agri-intelligence-platform`，使用 `docker-compose.server.yml` 覆盖本地配置。服务器上已成功运行：`agri-postgres`（容器内部 5432，不对外暴露）、`agri-backend`（宿主机 8003 -> 容器 8000）、`agri-frontend`（宿主机 3001 -> 容器 3000）。

服务器本机最后一次验证结果：三个容器运行，PostgreSQL healthy，健康检查正常，登录和 Dashboard 接口正常，自动种子数据成功写入，RAGFlow 状态接口可用。

服务器公网入口：前端 `http://47.105.74.222:3001`，API 文档 `http://47.105.74.222:8003/docs`，健康检查 `http://47.105.74.222:8003/health`。

服务器系统 firewalld 已放行 TCP 3001 和 TCP 8003；最后一次从开发机访问公网地址仍未打通，需在阿里云 ECS 安全组入方向放行 TCP 3001、TCP 8003。不要开放 5432。部署时不要停止或重建服务器上其他项目的容器。

## 8. 当前已知问题和接手建议

1. 本机直接运行 `pytest -q` 如果调用了全局 Python，可能因为 F 盘环境中的 NumPy/SciPy ABI 不匹配而在收集阶段失败。使用项目 `.venv` 解释器；Docker 后端依赖环境正常。
2. 服务器上的 RAGFlow 真实问答响应偏慢，系统会在超时后返回本地 TF-IDF 答案。若要稳定使用远程模型，应检查 RAGFlow 服务负载、网络、聊天助手配置和超时策略。
3. IP102 和 PlantDoc 都是公开数据集，不等于山东田间数据。后续最有价值的工作是采集本地作物、光照、手机拍摄和不同生育期样本，按类别补充标注并重新训练或微调。
4. `MOCK_DISEASE_MODEL`、`DISEASE_MODEL_PROVIDER`、`PEST_YOLO_MODEL_PATH` 控制识别模型。更换真实模型时保持 `predict(image_bytes)` 返回字段兼容，避免影响前端和 API。
5. 地图底图使用在线瓦片，离线或网络受限时可能不显示，但地块 polygon、风险颜色和业务信息仍可展示。正式答辩应准备离线截图或本地底图方案。
6. 正式上线前应更换 `SECRET_KEY`、演示密码和 RAGFlow/API 密钥，限制 CORS 和云安全组来源，并配置 HTTPS、备份、日志轮转和数据库持久化策略。
7. `submission_artifacts/final_upload/` 中的材料是生成快照；重新修改文档或部署配置后，应重新执行材料生成脚本并核对最终上传目录，避免提交旧版本源代码压缩包。

## 9. 推荐接手顺序

1. 先阅读本文件、`README.md`、`docs/安装部署说明.md` 和 `docs/现场演示与部署核验清单.md`。
2. 使用 `.venv` 启动后端，确认 `/health`、登录、Dashboard 和助手状态。
3. 启动前端，逐页检查 `/dashboard`、`/map`、`/disease`、`/predictions`、`/alerts`、`/inspections`、`/assistant`、`/reports`。
4. 任何模型改动先在 `data/generated/models/` 旁保存指标和版本说明，再接入 API。
5. 任何部署改动先检查 Compose 配置和已有端口，不要触碰服务器其他 Compose 项目。
6. 参赛展示优先保证业务闭环、数据口径、模型边界和演示稳定性，再做视觉细节扩展。
